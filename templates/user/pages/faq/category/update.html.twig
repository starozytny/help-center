{% extends 'user/pages/products/template.html.twig' %}

{% set page_title %}Modifier : {{ elem.name }}{% endset %}
{% set page_subtitle %}Modifiez les informations de la FAQ de {{ product.name }} : {{ elem.name}}{% endset %}

{% block breadcrumb %}
    {% set breadcrumbs = breadcrumbs | merge([
        { text: product.name, url: path('user_help_product_read', {'slug': product.name})  },
        { text: page_title }
    ]) %}
    {{ parent() }}
{% endblock %}

{% block main_content %}
    {% include "layout/components/admin/page_toolbar.html.twig" with { page_back: path('admin_help_faq_index', {'cat': elem.id}) } %}

    <div id="help_faq_category_update"
         data-obj="{{ obj }}"
         data-product-slug="{{ product.slug }}"
    >
        {% include "layout/components/loader.html.twig" %}
    </div>
{% endblock %}
